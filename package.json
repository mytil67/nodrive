{
  "name": "nodrive",
  "version": "0.1.0",
  "private": true,
  "description": "NoDrive — transfert de fichiers temporaire chiffré via Vercel",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/blob": "^0.23.4"
  },
  "devDependencies": {
    "vercel": "^37.5.0"
  }
}
